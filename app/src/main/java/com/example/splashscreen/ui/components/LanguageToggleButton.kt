package com.example.splashscreen.ui.components

import androidx.compose.runtime.Composable
import androidx.compose.ui.graphics.Color

/**
 * Language switching is intentionally disabled for now.
 * The component remains available so existing code will still compile
 * if another screen references it later.
 */
@Composable
fun LanguageToggleButton(
    textColor: Color = Color.White
) {
    // Disabled temporarily.
}